# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables — never commit. Use .env.example as the template.
.env
.env.local
.env.*.local

# Netlify
.netlify

# Subagent extraction debris (delete in Finder when convenient)
extract_large_files.py
EXTRACT_FILES_MANUAL.md

# Leftover BTF source assets (renamed/moved to /fonts, /media, /brand)
# Keeps the dl-side print flyers + AdobeStock out of the deploy.
public/assets/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
